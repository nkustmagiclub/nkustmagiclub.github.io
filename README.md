# 高科大魔術社長期入口網站

> **平常只改 `EDIT_CONTENT.md`；換圖片放 `images/`；想改顏色才碰 `EDIT_THEME.css`；其他不要動。**

1. 改文字／連結：編輯 `EDIT_CONTENT.md`，照檔案最上方範例。
2. 換主圖：放入 `images/hero.jpg`；沒有主圖也能正常顯示。
3. 改顏色：只改 `EDIT_THEME.css` 冒號後面的值。

內容送出後，GitHub Pages 會自動更新；通常等待幾分鐘再重新整理即可。
